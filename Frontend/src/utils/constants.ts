import type { LogLevel } from "../types/log";

export const LogLevelOptions: LogLevel[] = ["error", "warning", "info", "debug"];
// Base URL for API requests
export const BASE_URL = "http://localhost:3000/api/v1/logs";